# Скопируйте в .env.local и заполните значения.

# Сайт — статический экспорт (output: "export", Cloudflare Pages), сервера нет.
# Заявки уходят напрямую в мессенджер по ссылке (см. src/lib/lead/transport.ts,
# src/config/site.ts) — переменных окружения для их доставки не требуется.
