import ru from "../../messages/ru.json";

/**
 * Прямой доступ к строкам ru.json до подключения next-intl (этап 9).
 * Компоненты импортируют `ru` напрямую вместо хардкода строк в JSX.
 */
export { ru };
